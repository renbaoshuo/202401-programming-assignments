#include <stdio.h>

int main() {
    int math = 87;
    int eng = 72;
    int comp = 93;
    int average = (math + eng + comp) / 3;

    printf("math = %d, eng = %d, comp = %d, average = %d\n", math, eng, comp, average);

    return 0;
}
